[about]
author=AnonymerNiklasistanonym
description=Example for using a shared library inside Lua that rasterizes a vector image with a higher quality than the internal import and then draws it in the center of the current page
version=1.0

[default]
# Enabled per default
enabled=true

[plugin]
# The main script file
mainfile=main.lua
