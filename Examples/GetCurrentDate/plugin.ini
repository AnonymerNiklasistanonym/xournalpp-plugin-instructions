[about]
author=AnonymerNiklasistanonym
description=Get current date shortcut\nRegisters a toolbar icon that copies the current date to the clipboard\nRequires xclip to add the date to the clipboard!\n(add 'Plugin::GET_CURRENT_DATE_SHORTCUT' to a toolbar list in 'toolbar.ini' to get the toolbar icon)
version=1.0

[default]
# Disabled per default
enabled=false

[plugin]
# The main script file
mainfile=main.lua
